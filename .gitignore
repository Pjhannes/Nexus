node_modules/
.nexus/
.nexus-backups/
*.nexustmp
# Tauri-Build-Output (Rust-Target liegt zusaetzlich unter src-tauri/.gitignore).
src-tauri/target/
src-tauri/resources/
*.log
.DS_Store
Thumbs.db

# Persoenliche Live-Config (Pfade zum eigenen Vault) – nie ins Repo/GitHub.
# Vorlage: nexus.config.example.json. Wird zur Laufzeit vom Wizard erzeugt.
nexus.config.json

# --- Dev-Doku & lokale Helfer: bleiben LOKAL, nicht ins (GitHub-)Repo ---
# Projekt-Entscheidung (Session 43/44): persoenliche Entwickler-Notizen (enthalten
# Namen/Verlauf) und lokale Starthelfer werden aus dem Repo ausgeschlossen. Die
# Dateien bleiben auf der Platte (nur untracked) – nur fuer Commit/Push ignoriert.
STATUS.md
CLAUDE.md
rules/

# Lokale Starthelfer / Dev-Altlasten (Nexus-Dev.bat ist getrackt, der Rest bleibt lokal)
start-app.bat
restart.bat
setup.ps1
tmp-trunc.html
aufgaben Nexus.txt
*.lnk

# Design-Mockups (HTML-Entwuerfe + Logo-SVGs) – nicht Teil der App, enthalten Demo-Inhalt.
# Bleiben lokal, raus aus dem Repo (Session 46).
design-mockups/

# --- Claude Code ---
# .claude/launch.json IST im Repo (Dev-Server-Start, hilft auf jedem Rechner).
# Persoenliches bleibt lokal: die Permission-Allowlist enthaelt eigene Pfade/Befehle,
# worktrees/ sind temporaere Arbeitsverzeichnisse.
.claude/settings.local.json
.claude/worktrees/
